---
scope: WhatsApp sales-assistance pilot
purpose: build-substrate
altitude: feature
updated: 2026-07-14T22:24
---

- (decision) Pipes-and-filters workflows with PostgreSQL as the sole business-state authority prevent workflow-local state divergence.
- (decision) Inbound message IDs and outbound action keys are unique; conversation state advances with compare-and-set version checks.
- (decision) Single-instance n8n plus PostgreSQL for the pilot; add Redis queue mode and webhook processors only after measured capacity or availability requires them.
- (version) Architecture seed verified against n8n 2.25.7 stable evidence and PostgreSQL 17.10 supported release; recheck and pin container digests at implementation.
- (decision) Every customer send, including retry and fallback, passes one last authorization gate whose precedence starts with global stop and opt-out.
- (decision) Conversation ownership and lifecycle are orthogonal; only active plus AI-owned is automatable.
- (decision) Provider status is append-only and derived monotonically; older callbacks/dispatcher writes cannot regress current status.
- (decision) Invalid, low-confidence, conflicting-source, timeout, or provider-error model output transfers ownership to a human; only approved fallback text may acknowledge.
- (direction) Executive values remain open in PRD; architecture binds mechanisms but does not invent market, SLA, retention, or policy thresholds.
- (decision) Database-owned shared contracts, ingestion sequence, dispatch lease, knowledge safety revocation, and finite centralized retry policy close reviewer divergence findings.
- (version) n8n build candidate updated to 2.30.4 after current package verification; exact official image digest remains a release-manifest gate.
- (decision by user) Agent clients may author and verify n8n assets, but no chat or MCP state is a runtime/build dependency; each client uses its own verified configuration surface and all durable work lands in the repository.
- (decision by engineering) AD-14 now has one narrow ownership exception for the precommitted fixed Handoff acknowledgement; it is tied to the matching atomic transition, cannot receive a new logical key, and remains subordinate to opt-out.
- (decision by party-review) Pre-validation corrections added crash-expired dispatch reconciliation, one reproducible promotion/rollback story, typed Follow-Up policy ownership, and language/after-hours constraints on the sole Handoff acknowledgement exception.
