# N8N SalesFlow AI — Guided-Build Starter

This is the clean SalesFlow guided-build starter. Begin with:

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\learn.ps1 status
```

Continue with [learning/README.md](learning/README.md). The completed implementation is intentionally absent from this branch.

The learning track uses deterministic synthetic-local evidence. It does not prove production Meta, LLM, Handoff/CRM, managed database, legal, privacy, commercial, pilot, or owner-approval gates.
